# 第五章　像素、采样与数字图像

## 【核心摘要】

"相机的分辨率"是一个必须拆穿的用词陷阱——它只意味像素数量与像素尺寸；真正决定能否分开两个点的是光学系统，相机的职责是按奈奎斯特准则让最小分辨点横跨 2.5–3 个像素。本章同时讲透位深、动态范围、直方图与"显示调节不改变原始数据"的数字图像伦理。

## 【精编正文】

### **一、"相机的分辨率"：一个必须拆穿的词**

翻开任何一款相机的产品说明书，都有 resolution 一词。但请先做一个判断题：**物镜（光学系统）的分辨率，会受你所选相机的影响吗？**

答案是：不影响。这是两个截然不同的概念。

厂家口中的相机 resolution，不过是**一行多少像素、一列多少像素**——1392×1024 之类的阵列规格，正如你买手机时听到的"千万像素"。而第七章定义的真正的分辨率，指两个点经过光学系统发生衍射与干涉之后，**是否仍可被认为是两个点**——那完全由物镜与聚光镜决定。英文里两者泾渭分明：相机规格用 pixel size 和像素数，光学能力才用 resolution；中文把它们都译成"分辨率"，是混淆的源头。所以本书建议一个说话习惯：**说相机时，只说"我拍的是 2048×2048、像素尺寸 6.5 微米"，不说"相机的分辨率"。**

### **二、奈奎斯特准则：2.5–3 个像素的约定**

光学系统决定最小可分辨点的尺寸，相机的职责只剩一件：**采样时，让最小分辨点横跨 2.5 到 3 个像素。**这是香农–奈奎斯特采样定理在成像中的化身。为什么至少要两个多像素？设想两个相邻的亮点被拍进像素网格：若只落在两个像素上，两个亮点各占一格、中间无过渡，你仍无法确认那是两个点还是一个扁点；横跨三个像素时，亮—暗—亮的结构才得以呈现。

这条准则直接给出一道可计算的应用题。设你要解析的结构相距 300 纳米（0.3 微米），相机像素尺寸 6.45 微米，要求横跨 3 个像素：物镜需要把 0.3 微米放大到 3×6.45 = 19.35 微米，即放大倍数约为 19.35/0.3 ≈ **64.5 倍**——所以配 6.45 微米像素的相机，60 倍物镜恰好，40 倍则采样不足。注意这个计算的入口是"物镜放大倍数与相机的匹配"，而不是分辨率本身：**放大倍数决定采样是否匹配，数值孔径决定分辨率**——前者是几何，后者是物理。

采样不足，光学分辨率被白白浪费；采样过度，视野变小、每个像素摊到的光子变薄、文件变大——**过采样不是精益求精，而是白白摊薄了你的光子。**

### **三、位深与动态范围：你的像素有多"深"**

每个像素记录的灰度值，最终以二进制位数存储。2 位只有黑白灰四阶，图像形同剪影；8 位有 256 阶，勉强成图；科研相机常见 12 位（4096 级）与 14 位（16384 级）。图像文件通常以两字节（16 位，65536 级）存储——这解释了一个常见的困惑：**用 Windows 自带的看图软件打开一张 16 位的科研图像，往往一片漆黑**——65536 的空间里，你的信号只占了四千或一万出头， stretched 到整个灰阶上自然黑成一片。必须用专业软件（Fiji 等）打开，它会自动拉伸显示范围。

还要分清**位深与动态范围**：一台标称 14 位的相机，满阱容量折算后真实可用的动态范围可能只有 2667:1——模数转换的位数是桶的刻度，满阱电子数才是桶的容量。读相机说明书时，两个数字都要看。

### **四、直方图：一张图的体检报告**

直方图的 X 轴是灰度值，Y 轴是具有该灰度的像素个数。曝光不足的图，直方图挤在左侧；曝光合适，信号峰与背景基线分明；过曝的图，右端顶起一堵"墙"——那是所有被削顶到 65535 的像素，**饱和区的信号永远不可信**。实操原则是峰值控制在满量程的七八成。判断背景是否正常也有窍门：遮住光拍一张，像素应当均匀分布，没有异常亮点或暗点。

### **五、显示的伦理：一切"美颜"都不改变数据**

拍摄之后，你可以拉伸显示范围、调节伽马、一键自动对比度，让图像在屏幕上焕然一新。请牢记工程师反复强调的铁律：**这一切只改变显示，不改变原始数据。**原始文件里每个像素的灰度值纹丝不动；定量分析必须回到原始灰度值上进行。展示用图与定量用图是两套规则——论文里贴出的图可以调显示（且应注明调节方式），但你必须保留原始文件，因为审稿人与同行要复算的，永远是那条没有被"美颜"过的曲线。

由此也回答了"要不要给图像降噪"的疑问：科学层面，降噪没有必要——你知道背景是 1000，把它抹成 0 没有价值；而且你凭什么确定那是噪声？**原始数据在，机会就在。**

### **六、Binning：用分辨率换信噪比**

若信号实在太弱，相机还留了一手：binning。2×2 binning 把相邻四个像素合并为一个（6.5 微米变 13 微米），四份光子汇集一处，噪声经平均被压低，信噪比大致按像素数的平方根提升，同时帧率提高、文件变小。代价写在第五章的开头——像素变大，采样变粗，与奈奎斯特准则的匹配关系随之改变。**这是一笔可以做的交易，但下单前先算清账。**

### **七、补写：数字图像处理的基本概念**

（以下参照 Murphy & Davidson《Fundamentals of Light Microscopy and Electronic Imaging》数字图像章节补全，供深入阅读。）除上述内容外，完整的数字图像基础还应包括：**平场校正（flat-field correction）**——用空白视野图像扣除照明不均与灰尘阴影；**背景扣除**的代数规则——先减背景再做比值，顺序不可颠倒；**查找表（LUT）**与伪彩的本质——只是显示映射；**文件格式**——TIFF 无损而通用，JPEG 的有损压缩在科研图像中不可接受。这些操作在本书配套的 Fiji 练习中均有对应步骤。

## 【金句提炼】

1. 说相机时，只说"像素尺寸 6.5 微米、2048×2048"，不说"相机的分辨率"——前者是几何，后者是物理。
2. 让最小分辨点横跨 2.5 到 3 个像素——这是相机对光学系统应尽的本分。
3. 放大倍数决定采样是否匹配，数值孔径决定分辨率。
4. 饱和区的信号永远不可信——那条数据你永远不知道是多少。
5. 一切显示调节都只是"美颜"；审稿人要复算的，永远是原始灰度值。
6. Binning 是一笔可以做的交易，但下单前先算清账。
